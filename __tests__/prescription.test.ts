describe("Prescription", () => {
    it("should be able to create a new prescription", () => {
        const x = 2;
        const y = 4;
        const sum = x + y;

        expect(sum).toBe(6)
    })
})